{
  "name": "@litlbyte/acap",
  "repository": "git@github.com:ehildt/acap.git",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "prepare": "husky",
    "ncu:update": "npx npm-check-updates --doctor -u",
    "compose": "docpose -c ./.docpose.yml",
    "changeset": "npx changeset add",
    "clean": ".python/clean.sh",
    "test": "echo 'passing with no tests in ROOT!'"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "dotenv-cli": "^7.4.2",
    "husky": "^9.1.0",
    "npm-check-updates": "^16.14.20"
  },
  "dependencies": {
    "docpose": "^1.0.12"
  }
}