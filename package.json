{
  "name": "@litlbyte/acap",
  "repository": "git@github.com:ehildt/acap.git",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "prepare": "husky",
    "ncu:update": "npx npm-check-updates --doctor -u --format group --format group",
    "docpose": "docpose -c .docpose.yml",
    "changeset": "npx changeset add",
    "test": "echo 'passing with no tests in ROOT!'"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "dotenv-cli": "^7.4.2",
    "husky": "^9.1.4",
    "npm-check-updates": "^17.0.0"
  },
  "dependencies": {
    "docpose": "^1.0.12"
  }
}